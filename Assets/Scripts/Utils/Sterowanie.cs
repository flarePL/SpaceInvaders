
using System;
using SpaceInvaders.Elementy;
using UnityEngine;
using Zenject;


namespace SpaceInvaders.Utils
{
    
    public class Sterowanie : MonoBehaviour
    {
       

        private void Start()
        {
           
        }

        /*public void Update()
        {
            if (gameManager.stanGry == GameStates.PLAYING)
            {
                if (Input.GetKey(KeyCode.LeftArrow))
                {
                    statek.MoveLeft();
                } //OnPressKeyLeft?.Invoke();

                if (Input.GetKey(KeyCode.RightArrow))
                {
                    statek.MoveRight();
                } //OnPressKeyRight?.Invoke();
            }
        }*/
    }
}
